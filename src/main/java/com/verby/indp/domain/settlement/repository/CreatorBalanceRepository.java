package com.verby.indp.domain.settlement.repository;

import com.verby.indp.domain.settlement.CreatorBalance;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CreatorBalanceRepository extends JpaRepository<CreatorBalance, Long> {

}
